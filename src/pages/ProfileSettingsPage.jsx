import React from 'react';
import ProfilePage from './ProfilePage';

// This component is a wrapper for ProfilePage since the functionality is the same
const ProfileSettingsPage = () => {
  return <ProfilePage />;
};

export default ProfileSettingsPage;
